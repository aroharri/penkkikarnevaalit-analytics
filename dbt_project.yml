name: penkkikarnevaalit
version: "0.1.0"
config-version: 2

profile: penkkikarnevaalit

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  penkkikarnevaalit:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
